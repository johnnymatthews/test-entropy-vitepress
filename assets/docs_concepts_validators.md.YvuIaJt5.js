import{_ as e,c as r,o as t,a3 as a}from"./chunks/framework.7ep0oyv4.js";const o="/assets/validators-birds-eye.D7YV6aUc.png",f=JSON.parse('{"title":"Entropy Validator Nodes","description":"","frontmatter":{},"headers":[],"relativePath":"docs/concepts/validators.md","filePath":"docs/concepts/validators.md"}'),s={name:"docs/concepts/validators.md"},n=a('<h1 id="entropy-validator-nodes" tabindex="-1">Entropy Validator Nodes <a class="header-anchor" href="#entropy-validator-nodes" aria-label="Permalink to &quot;Entropy Validator Nodes&quot;">​</a></h1><p><a href="https://github.com/entropyxyz/entropy-core" target="_blank" rel="noreferrer"><code>entropy-core</code></a> is run by validator nodes in the Entropy network. It has two different binaries, both of which are run by each validator node:</p><ul><li>The Entropy blockchain, built with <a href="https://docs.substrate.io/" target="_blank" rel="noreferrer">Substrate</a>. This handles public data relating to who the users are and which programs they use, who the validators are and which &#39;signing subgroup&#39; they belong to, and the bytecode of all available programs.</li><li>The <a href="https://github.com/entropyxyz/entropy-core/tree/master/crates/threshold-signature-server" target="_blank" rel="noreferrer">Threshold signature server</a> which has an HTTP API based on <a href="https://docs.rs/axum" target="_blank" rel="noreferrer">Axum</a>. This handles private data such a user&#39;s signing keyshares, and runs the signing protocol.</li></ul><p>If you are interested in how to interact with the Entropy network, rather than how it works on the back end, you might want to jump to the SDK documentation.</p><p><img src="'+o+'" alt="birdsEye"></p><h2 id="the-entropy-chain-src-api" tabindex="-1">The Entropy chain <a href="https://github.com/entropyxyz/entropy-core" target="_blank" rel="noreferrer">src</a> <a href="https://docs-api-entropy-core.vercel.app/entropy" target="_blank" rel="noreferrer">API</a> <a class="header-anchor" href="#the-entropy-chain-src-api" aria-label="Permalink to &quot;The Entropy chain [src](https://github.com/entropyxyz/entropy-core) [API](https://docs-api-entropy-core.vercel.app/entropy)&quot;">​</a></h2><p>The purpose of the Entropy blockchain is to have a &#39;single source of truth&#39; for the information which needs to be public and which the threshold signature servers need to have consensus on. For example, we need to have agreement of which validators belong to which signing subgroups, and which subgroups will participate in signing a particular message.</p><h3 id="general-functionality-from-substrate-polkadot" tabindex="-1">General functionality from Substrate / Polkadot: <a class="header-anchor" href="#general-functionality-from-substrate-polkadot" aria-label="Permalink to &quot;General functionality from Substrate / Polkadot:&quot;">​</a></h3><ul><li>Uses the <strong><a href="https://research.web3.foundation/en/latest/polkadot/block-production/Babe.html" target="_blank" rel="noreferrer">BABE consensus algorithm</a></strong> (Blind Assignment for Blockchain Extension). A brief description of BABE: <ul><li>Time is divided into &#39;epochs&#39; which consist of a series of &#39;slots&#39; for each block that will be published.</li><li>The genesis block contains a random value which determines which nodes will produce blocks during the first two &#39;epochs&#39;. After that, each epoch uses &#39;randomness&#39; from two epochs ago in a kind of &#39;lottery&#39;, using a &#39;verifiable random function&#39; to decide whether a given node may produce a block for a given slot.</li><li>Some slots have no block producer chosen - in which case a producer is chosen by another selection algorithm - &#39;round robin&#39;.</li><li>Some slots have several block producers chosen - in which case all chosen nodes produce a block and there is a &#39;race&#39; between forks, and the finalisation protocol determines which is kept.</li></ul></li><li>Finality is determined by <strong><a href="https://github.com/w3f/consensus/blob/master/pdf/grandpa.pdf" target="_blank" rel="noreferrer">Grandpa</a></strong> (GHOST-based Recursive ANcestor Deriving Prefix Agreement). Finality is the process by which the network agrees that a block will never be reverted.</li><li>The blockchain runtime compiles to WASM, which allows updates to be published on-chain and carried out automatically without requiring hard forking.</li><li>Nodes discover each other via <a href="https://github.com/libp2p/specs/blob/master/kad-dht/README.md" target="_blank" rel="noreferrer">libp2p&#39;s kademlia DHT</a>.</li></ul><h3 id="custom-functionality-specific-to-entropy" tabindex="-1">Custom functionality specific to Entropy: <a class="header-anchor" href="#custom-functionality-specific-to-entropy" aria-label="Permalink to &quot;Custom functionality specific to Entropy:&quot;">​</a></h3><ul><li><strong>Staking extension pallet</strong> <a href="https://github.com/entropyxyz/entropy-core/blob/master/pallets/staking/src/lib.rs" target="_blank" rel="noreferrer">src</a> <a href="https://docs-api-entropy-core.vercel.app/pallet_staking_extension/index.html" target="_blank" rel="noreferrer">API</a> - staking is extended to assign a particular Threshold Signature Servers account to a particular chain node, and tracks which signing subgroup they belong to.</li><li><strong>Relayer pallet</strong> <a href="https://github.com/entropyxyz/entropy-core/blob/master/pallets/relayer/src/lib.rs" target="_blank" rel="noreferrer">src</a> <a href="https://docs-api-entropy-core.vercel.app/pallet_relayer/index.html" target="_blank" rel="noreferrer">API</a> - This provides a registry of Entropy users, and which programs are currently associated with their account. This uses substrate <a href="https://docs.substrate.io/build/events-and-errors" target="_blank" rel="noreferrer">events</a>.</li><li><strong>Programs pallet</strong> <a href="https://github.com/entropyxyz/entropy-core/blob/master/pallets/programs/src/lib.rs" target="_blank" rel="noreferrer">src</a> <a href="https://docs-api-entropy-core.vercel.app/pallet_programs/index.html" target="_blank" rel="noreferrer">API</a> - This stores program bytecode as well as metadata associated with the program such as a description of its interface and how many times it is used.</li><li><strong>Free transactions pallet</strong> <a href="https://github.com/entropyxyz/entropy-core/tree/master/pallets/free-tx" target="_blank" rel="noreferrer">src</a> <a href="https://docs-api-entropy-core.vercel.app/pallet_free_tx/index.html" target="_blank" rel="noreferrer">API</a> - free transactions are also known as &#39;zaps&#39;.</li></ul><h2 id="the-threshold-signature-server-src-api" tabindex="-1">The Threshold Signature Server <a href="https://github.com/entropyxyz/entropy-core/tree/master/crates/threshold-signature-server" target="_blank" rel="noreferrer">src</a> <a href="https://docs-api-entropy-core.vercel.app/entropy_tss/index.html" target="_blank" rel="noreferrer">API</a> <a class="header-anchor" href="#the-threshold-signature-server-src-api" aria-label="Permalink to &quot;The Threshold Signature Server [src](https://github.com/entropyxyz/entropy-core/tree/master/crates/threshold-signature-server) [API](https://docs-api-entropy-core.vercel.app/entropy_tss/index.html)&quot;">​</a></h2><p>This is the part which carries out the threshold signing protocol, together with other instances of the threshold signature server. It has an encrypted key-value store used for private information where consensus is not required. Since the threshold signature server deals with private data which must never be exposed publicly on-chain, it is distributed as a separate binary. It also handles the distributed key generation and proactive-refresh protocols.</p><p>It has the following features:</p><ul><li>The <strong>signing client</strong> <a href="https://github.com/entropyxyz/entropy-core/tree/master/crates/threshold-signature-server/src/signing_client" target="_blank" rel="noreferrer">src</a> which handles listeners for the different protocol sessions. The protocol transport is handled by the <strong>entropy-protocol</strong> crate <a href="https://github.com/entropyxyz/entropy-core/tree/master/crates/protocol" target="_blank" rel="noreferrer">src</a> <a href="https://docs-api-entropy-core.vercel.app/entropy_protocol/index.html" target="_blank" rel="noreferrer">API</a> which run the [ThresholdSignaureScheme].</li><li>An <strong>encrypted key-value store</strong> <a href="https://github.com/entropyxyz/entropy-core/tree/master/crypto/kvdb" target="_blank" rel="noreferrer">src</a> <a href="https://docs-api-entropy-core.vercel.app/kvdb/index.html" target="_blank" rel="noreferrer">API</a> for key shares and other secret data, which are submitted by the user. Built with <a href="https://docs.rs/sled/latest/sled" target="_blank" rel="noreferrer">sled</a>.</li><li>Executes <a href="./programs.html">programs</a> - upon which a decision is made as to whether to participate in signing a given message.</li><li>An <strong><a href="https://docs-api-entropy-core.vercel.app/entropy_tss" target="_blank" rel="noreferrer">HTTP API</a></strong> for communication with users, with the entropy chain node, and with other threshold servers.</li><li>An account for submitting extrinsics (transactions) to the Entropy chain. For example, when the distributed key generation protocol runs successfully during user registration, each TSS server sends a confirmation to the chain by submitting a transaction.</li></ul><h3 id="usage" tabindex="-1">Usage <a class="header-anchor" href="#usage" aria-label="Permalink to &quot;Usage&quot;">​</a></h3><p><code>entropy-tss</code> is a member of the <code>entropy-core</code> workspace. When you run <code>entropy-tss</code>, you will be asked for a password used to encrypt the key-value store.</p><p>Be aware there is no way to recover this password if you lose it.</p><p>The database is stored in the <code>.entropy</code> directory, which is created in the current working directory where the binary is run. You can remove this directory if you need to &#39;start fresh&#39; during development.</p><p>If <code>entropy-tss</code> is compiled with the <code>unsafe</code> feature enabled, some extra HTTP routes will be made available. These are for testing and development purposes only, and allow direct access to the key-value store.</p><p>If you need these, build with:</p><p><code>cargo build -p entropy-tss --release --features unsafe</code></p>',22),i=[n];function h(l,c,p,d,u,g){return t(),r("div",null,i)}const y=e(s,[["render",h]]);export{f as __pageData,y as default};
