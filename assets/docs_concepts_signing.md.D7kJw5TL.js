import{_ as e,c as t,o as s,a3 as i}from"./chunks/framework.7ep0oyv4.js";const n="/assets/signing-signing-new.BeRiOgPo.svg",f=JSON.parse('{"title":"Signing","description":"","frontmatter":{},"headers":[],"relativePath":"docs/concepts/signing.md","filePath":"docs/concepts/signing.md"}'),o={name:"docs/concepts/signing.md"},r=i('<h1 id="signing" tabindex="-1">Signing <a class="header-anchor" href="#signing" aria-label="Permalink to &quot;Signing&quot;">​</a></h1><p>The signing process can only take place when a user is already registered on Entropy. The Process involves having the user and a committee of validators collectively perform the signing.</p><p>The committee requires one Validator from each signing group as well as the user. The current version of the program will be executed here to determine whether or not to proceed with the signing protocol.</p><p>For details on how signatures are actually created, see <a href="./threshold-signature-scheme.html">Threshold Signature Scheme</a>.</p><h2 id="signing-process" tabindex="-1">Signing process <a class="header-anchor" href="#signing-process" aria-label="Permalink to &quot;Signing process&quot;">​</a></h2><p><img src="'+n+'" alt="Signing Flow New"></p><ol><li>The user computes the hash of the message they wish to sign, and selects a signing committee by deterministically selecting a member of each signing group based on this hash. They can get the details of the signing groups as they were published on chain when the user <a href="./registering.html">registered</a>.</li><li>The user contacts all threshold servers in the signing committee and makes a POST to <code>/user/sign_tx</code> with the message to be signed (encrypted for that node).</li><li>On receiving a message, each node checks that it is a member of the signing committee for that message using the hash.</li><li>The Threshold server retrieves the latest version of the associated program from the entropy chain, and executes it with the message to be signed as input. Only on getting successful program output do they continue to the next step.</li><li>The Threshold server sets up websocket connections to or from the rest of the committee to use for threshold signing protocol messages. They decide whether to make an outgoing connection, or accept an incoming one by comparing account IDs. These connections are secured using the <a href="https://noiseprotocol.org/noise.html" target="_blank" rel="noreferrer">noise protocol</a>. Signing protocol messages can be either &#39;broadcast&#39; to all of the committee or &#39;p2p&#39; to a specific member.</li><li>Once all members of the signing committee have subscribed, nodes participate in the signing protocol to produce a signature, using the key-shares retrieved from their key-value store.</li><li>If the signing process fails, nodes broadcast who the malicious/faulty signer was, which is included in the next block. Following that, the next block contains details of a new signing committee. The misbehaving signer will be &#39;slashed&#39; (not yet implemented).</li><li>If the process is successful, the signature is returned to the user. Currently this requires the user to repeatedly poll POST <code>signer/signature</code> with the signature hash, until it successfully retrieves a signature.</li></ol>',7),a=[r];function h(c,g,l,d,m,p){return s(),t("div",null,a)}const _=e(o,[["render",h]]);export{f as __pageData,_ as default};
